# dbt project — added Week 2

Staging models will clean the `raw.*` tables (typed columns, deduped realtime
polls). Mart models will compute on-time performance, headway variability,
and route-level delay summaries. Not yet initialized — run `dbt init` here
once the realtime tables have data to transform.
